__pycache__/
*.py[cod]
.venv/
venv/
env/

# Note: sentiment.db and data.json are deliberately NOT ignored —
# the collect-sentiment workflow commits them back to the repo, and
# data.json's raw URL is the live feed.
